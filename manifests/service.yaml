apiVersion: v1
kind: Service
metadata:
  name: virtual-dm-service
  labels:
    app: virtual-dm
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: virtual-dm
