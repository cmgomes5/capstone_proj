name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  ci-cd:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 #v5
    
    - name: Build
      run: echo "build"
    
    - name: Test
      run: echo "test"
    
    - name: Deploy
      run: echo "deploy"
